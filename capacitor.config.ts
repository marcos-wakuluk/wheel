import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.wheelfortune',
  appName: 'wheel-of-fortune',
  webDir: 'build'
};

export default config;
